package regex;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class RepeatedPattern {

	static String outputString;
	public static void main(String[] args) {

		Pattern pattern =Pattern.compile("(a)\\1+");
		Matcher matcher= pattern.matcher("ab5aaaba");
		while(matcher.find()){
			System.out.println(matcher.start()+" "+matcher.group());
		}
		
		outputString = new StringBuilder("aaaaaaaaabbbbbbbbcddggaaybbbby").reverse().toString();
		System.out.println("Before: "+outputString);
		outputString = outputString.replace("(.)(?=.*\\1)*", "");
		System.out.println("After: "+outputString);
		outputString = new StringBuilder(outputString).reverse().toString();
		System.out.println(outputString);
	}

}
